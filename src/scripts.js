document.getElementById('create-event-form').addEventListener('submit', function (event) {
    event.preventDefault();
    // 在这里处理发布聚会表单数据
    console.log('发布聚会表单已提交');
  });
  
  document.getElementById('search-event-form').addEventListener('submit', function (event) {
    event.preventDefault();
    // 在这里处理搜索聚会表单数据
    console.log('搜索聚会表单已提交');
  });
  
  document.getElementById('match-event-form').addEventListener('submit', function (event) {
    event.preventDefault();
    // 在这里处理聚会匹配表单数据
    console.log('聚会匹配表单已提交');
  });
  
  document.getElementById('review-form').addEventListener('submit', function (event) {
    event.preventDefault();
    // 在这里处理评估系统表单数据
    console.log('评估系统表单已提交');
  });
  